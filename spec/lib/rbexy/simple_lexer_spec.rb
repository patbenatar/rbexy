RSpec.describe Rbexy::SimpleLexer do
  context "focus" do
    # it "tokenizes text as raw" do
    #   subject = Rbexy::SimpleLexer.new("Hello world")
    #   expect(subject.tokenize).to eq [[:RAW, "Hello world"]]
    # end

    # it "tokenizes known html tags as raw" do
    #   subject = Rbexy::SimpleLexer.new("<div></div>")
    #   expect(subject.tokenize).to eq [[:RAW, "<div></div>"]]
    # end
  end
end
